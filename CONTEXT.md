# dsh-simple-orchestrator 上下文

一个 DeepSeek Harness 的 Agent 预设：顶层 Agent 自己不动代码，只做理解、拆解、委派与汇总；代码读写全部落在它派生的子代理身上。本文件是项目领域语言的术语表。

## 角色与拓扑

**编排器（Orchestrator）**：
本预设的顶层 Agent。它唯一的职责是理解意图、拆解任务、写出自包含的委派提示、汇总子代理的回报；它自己不读文件、不写文件、不执行命令。
_Avoid_: 主 Agent、supervisor、manager

**工作子代理（Worker）**：
由编排器派生、真正读写代码与执行命令的子代理。它加入编排器所在的预设，因此天然拥有完整编码工具集，也可以继续委派。
_Avoid_: 子任务、executor、child agent

**顶层会话（Top-level session）**：
用户直接创建、运行编排器的会话。与由委派产生的子代理会话相对，后者永远不会成为编排器。
_Avoid_: 主会话、parent session

**委派（Delegation）**：
编排器把一件自包含的工作交给工作子代理、并收回其结果的动作。委派提示必须自带目标、工作目录、关注的文件与回报要求，因为子代理看不见编排器的对话。
_Avoid_: 分发、转发、subtask

**继承式组合（Inherited composition）**：
子代理加入其父 Agent 所在预设的机制。因此子代理的能力只会是「父预设挂载的行」加上它自身的少量附加能力，不可能凭空多出父亲没有的工具。
_Avoid_: 能力继承、scope 继承

## 工具面

**工具掩码（Tool Mask）**：
把读写代码与执行命令的能力从编排器的工具目录里抹掉的机制。它只作用于顶层 Agent，对工作子代理没有任何影响。
_Avoid_: 权限过滤、黑名单、sandbox

**保留清单（KEEP）**：
编排器明确允许看见的工具集合：skill 读取、向用户提问、联网搜索与抓取、进度清单、计划评审、目标、交付物、后台任务控制，以及全部子代理交互工具。
_Avoid_: allowlist

**拒绝清单（DENY）**：
掩码明确抹掉的工具集合，即每一个能读文件、写文件或执行命令的工具。
_Avoid_: denylist、黑名单

**编排放大工具（Orchestration amplifiers）**：
`workflow` 与 `ralph`：把一次委派放大成多代理扇出或新鲜 Agent 迭代循环的工具。它们属于编排器，工作子代理在需要时也可以自行使用。
_Avoid_: 批处理、流水线

## 预设与分发

**预设（Agent preset）**：
一个目录，描述「一次会话由哪些插件行组合而成」。本项目的预设还携带自己的一个 JS 文件（掩码行），因此整份目录被复制走之后语义不变。
_Avoid_: 配置、模板；口语里的「编排模式」指本预设的展示名

**预设根（Preset root）**：
roster 扫描的目录，其中每个子目录是一个预设。本项目的预设根随安装包分发，由 bundle 层注入到 roster。
_Avoid_: 预设目录（指单个预设自身时不要用这个词）

**组合行（Composition row）**：
`agent.cordis.yml` 里的一行插件挂载声明。挂载什么就等于赋予什么，所以「预设有多可信」等价于「它挂了哪些行」。
_Avoid_: 插件条目、entry

**预设自带行（Preset-carried row）**：
由预设目录内的相对路径引用的组合行。它让预设成为自包含的单位：目录搬到哪里，行的代码就跟到哪里。
_Avoid_: 本地插件、内联插件

**bundle 层（Bundle layer）**：
安装包提供给 profile 的一个 patch 层。本项目的层只做一件事：把预设根加进 roster，让预设出现在选择器里。
_Avoid_: 插件包、依赖

## 提示词

**提示词足迹（Prompt footprint）**：
预设自己注入模型请求的常驻文字。它只陈述工具描述表达不了的事：编排器自己缺哪些能力、工作子代理有什么能力、以及回报纪律。工具怎么用、参数是什么、什么时候该用，属于工具自己的描述，不在这里重复一遍——两处都写，只让两边各自漂移。
_Avoid_: 系统提示词、persona（指展示名时不要混用）

**回报纪律（Reporting rule）**：
编排器只能说出子代理或工具真的返回过的东西。它自己读不到文件，所以「据某文件所示」这类话只能是转述；没有子代理确认过，就不能说改动已经发生。
_Avoid_: 引用规范、事实核查
